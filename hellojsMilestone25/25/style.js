let selectors  = document.querySelectorAll('section');
// console.log(qS)
for (const selector of selectors) {
    selector.style.border = '2px solid steelblue';
    selector.style.marginBottom = "6px";
}